```react
import React, { useState, useEffect, useRef } from 'react';
import { 
  Video, Mic, MicOff, VideoOff, MonitorUp, 
  MessageSquare, PhoneOff, Play, Clock, 
  Users, FileText, CheckCircle2, Share2, 
  Download, ChevronRight, Home, LayoutDashboard,
  User, Settings, Bell, Search, X, LogOut,
  BarChart3, Layout
} from 'lucide-react';

const MOCK_TRANSCRIPT = [
  { id: 1, speaker: "Jane Doe", time: "10:02 AM", text: "Welcome everyone to the Q3 planning sync." },
  { id: 2, speaker: "John Smith", time: "10:03 AM", text: "Thanks Jane. Let's look at the new dashboard metrics I'm sharing on screen." },
  { id: 3, speaker: "Jane Doe", time: "10:04 AM", text: "The engagement numbers in Nairobi look fantastic. We need to replicate this." },
  { id: 4, speaker: "Sarah Lee", time: "10:05 AM", text: "I'll take that as an action item. I'll draft the expansion strategy by Thursday." },
  { id: 5, speaker: "John Smith", time: "10:06 AM", text: "Perfect. I'll stop sharing my screen now so we can wrap up." },
];

const ACTION_ITEMS = [
  "Review new dashboard metrics for Q3.",
  "Sarah to draft the expansion strategy based on Nairobi engagement by Thursday.",
  "Schedule follow-up sync for Q4 projections."
];

export default function StratumHouseApp() {
  // Main App Navigation State: 'home', 'video', 'messages', 'profile'
  const [activeTab, setActiveTab] = useState('home');
  
  // Call Session State: 'idle', 'active', 'summary'
  const [callStatus, setCallStatus] = useState('idle');
  
  // In-Call States
  const [isMuted, setIsMuted] = useState(false);
  const [isVideoOff, setIsVideoOff] = useState(false);
  const [showTranscript, setShowTranscript] = useState(true);
  const [isScreenSharing, setIsScreenSharing] = useState(false);
  const [showEndCallModal, setShowEndCallModal] = useState(false);
  
  const [liveTranscript, setLiveTranscript] = useState([]);
  const transcriptEndRef = useRef(null);

  useEffect(() => {
    if (callStatus === 'active') {
      setLiveTranscript([]);
      let currentIndex = 0;
      
      const interval = setInterval(() => {
        if (currentIndex < MOCK_TRANSCRIPT.length) {
          setLiveTranscript(prev => [...prev, MOCK_TRANSCRIPT[currentIndex]]);
          
          // Auto-trigger screen share for simulation purposes at message 2
          if (currentIndex === 1) setIsScreenSharing(true);
          // End screen share at message 4
          if (currentIndex === 4) setIsScreenSharing(false);
          
          currentIndex++;
        } else {
          clearInterval(interval);
        }
      }, 3500);

      return () => clearInterval(interval);
    }
  }, [callStatus]);

  useEffect(() => {
    if (transcriptEndRef.current) {
      transcriptEndRef.current.scrollIntoView({ behavior: 'smooth' });
    }
  }, [liveTranscript]);

  const renderHome = () => (
    <div className="p-6 md:p-8 max-w-5xl mx-auto w-full pb-28">
      <header className="flex justify-between items-center mb-10">
        <div className="flex items-center gap-3">
          <div className="w-10 h-10 bg-blue-600 rounded-xl flex items-center justify-center shadow-lg shadow-blue-200">
            <Layout className="text-white w-6 h-6" />
          </div>
          <h1 className="text-2xl font-bold text-slate-900 tracking-tight">Stratum House</h1>
        </div>
        <div className="flex items-center gap-4">
          <button className="relative p-2 text-slate-400 hover:text-slate-600 transition-colors">
            <Bell className="w-6 h-6" />
            <span className="absolute top-1 right-1 w-2.5 h-2.5 bg-red-500 rounded-full border-2 border-slate-50"></span>
          </button>
        </div>
      </header>

      <div className="grid lg:grid-cols-5 gap-8">
        <div className="lg:col-span-3 bg-white p-8 rounded-3xl shadow-sm border border-slate-100 flex flex-col justify-center relative overflow-hidden">
          <div className="absolute top-0 right-0 w-64 h-64 bg-blue-50 rounded-full blur-3xl -mr-20 -mt-20 opacity-50 pointer-events-none"></div>
          <h2 className="text-3xl font-bold text-slate-900 mb-4 relative z-10">Meet with context.</h2>
          <p className="text-slate-500 mb-8 leading-relaxed relative z-10 max-w-md">
            Start an instantly transcribed video session. Searchable records, smart screen sharing, and automatic action items.
          </p>
          <div className="flex flex-col sm:flex-row gap-4 relative z-10">
            <button 
              onClick={() => setCallStatus('active')}
              className="bg-blue-600 hover:bg-blue-700 text-white font-semibold py-4 px-6 rounded-2xl flex items-center justify-center gap-2 transition-all duration-200 shadow-lg shadow-blue-600/20"
            >
              <Video className="w-5 h-5" />
              New Meeting
            </button>
            <button className="bg-slate-100 hover:bg-slate-200 text-slate-700 font-semibold py-4 px-6 rounded-2xl flex items-center justify-center gap-2 transition-all duration-200">
              <Calendar className="w-5 h-5" />
              Schedule
            </button>
          </div>
        </div>

        <div className="lg:col-span-2 bg-white p-6 rounded-3xl shadow-sm border border-slate-100">
          <div className="flex items-center justify-between mb-6">
            <h3 className="font-semibold text-slate-900">Upcoming Today</h3>
          </div>
          <div className="space-y-4">
            {[
              { title: 'Q3 Planning Sync', time: '10:00 AM', team: 'Executive' },
              { title: 'Vendor Review: AWS', time: '1:30 PM', team: 'Engineering' },
              { title: 'Nairobi Office Design', time: '4:00 PM', team: 'Operations' },
            ].map((meeting, i) => (
              <div key={i} className="flex items-center p-3 rounded-2xl hover:bg-slate-50 border border-transparent hover:border-slate-100 transition-colors cursor-pointer group">
                <div className="w-12 h-12 rounded-xl bg-blue-50 text-blue-600 flex items-center justify-center mr-4">
                  <Clock className="w-5 h-5" />
                </div>
                <div className="flex-1">
                  <h4 className="font-semibold text-slate-900 group-hover:text-blue-600 transition-colors">{meeting.title}</h4>
                  <p className="text-sm text-slate-500">{meeting.time} • {meeting.team}</p>
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  );

  const renderMessages = () => (
    <div className="p-6 md:p-8 max-w-3xl mx-auto w-full pb-28">
      <h2 className="text-2xl font-bold text-slate-900 mb-6">Messages</h2>
      <div className="relative mb-6">
        <Search className="w-5 h-5 absolute left-4 top-1/2 -translate-y-1/2 text-slate-400" />
        <input 
          type="text" 
          placeholder="Search conversations..." 
          className="w-full bg-white border border-slate-200 rounded-2xl py-3 pl-12 pr-4 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all"
        />
      </div>
      <div className="space-y-3">
        {[
          { name: 'Sarah Lee', msg: 'The integration is ready for testing.', time: '10:45 AM', unread: true },
          { name: 'John Smith', msg: 'Can we review the Q4 projections later?', time: 'Yesterday', unread: false },
          { name: 'Engineering Team', msg: 'AWS deployment successful.', time: 'Tuesday', unread: false },
        ].map((chat, i) => (
          <div key={i} className="flex items-center p-4 bg-white rounded-2xl shadow-sm border border-slate-100 hover:border-blue-200 cursor-pointer transition-colors">
            <div className="w-12 h-12 rounded-full bg-slate-100 flex items-center justify-center font-bold text-slate-500 mr-4">
              {chat.name.substring(0, 2).toUpperCase()}
            </div>
            <div className="flex-1">
              <div className="flex justify-between items-baseline mb-1">
                <h4 className={`font-semibold ${chat.unread ? 'text-slate-900' : 'text-slate-700'}`}>{chat.name}</h4>
                <span className="text-xs text-slate-400">{chat.time}</span>
              </div>
              <p className={`text-sm ${chat.unread ? 'text-slate-900 font-medium' : 'text-slate-500'}`}>{chat.msg}</p>
            </div>
            {chat.unread && <div className="w-3 h-3 bg-blue-600 rounded-full ml-4"></div>}
          </div>
        ))}
      </div>
    </div>
  );

  const renderProfile = () => (
    <div className="p-6 md:p-8 max-w-2xl mx-auto w-full pb-28">
      <h2 className="text-2xl font-bold text-slate-900 mb-6">Profile & Settings</h2>
      <div className="bg-white rounded-3xl shadow-sm border border-slate-100 overflow-hidden">
        <div className="p-8 border-b border-slate-100 flex items-center gap-6">
          <div className="w-24 h-24 rounded-2xl bg-blue-100 text-blue-600 flex items-center justify-center text-3xl font-bold">
            JD
          </div>
          <div>
            <h3 className="text-2xl font-bold text-slate-900">Jane Doe</h3>
            <p className="text-slate-500">jane.doe@stratumhouse.com</p>
            <div className="mt-3 inline-flex items-center gap-1.5 bg-blue-50 text-blue-700 px-3 py-1 rounded-lg text-sm font-medium border border-blue-100">
              <CheckCircle2 className="w-4 h-4" /> Pro Workspace
            </div>
          </div>
        </div>
        <div className="p-4">
          {[
            { icon: User, label: 'Account Details' },
            { icon: Settings, label: 'Preferences & Audio' },
            { icon: Users, label: 'Workspace Team' },
            { icon: LogOut, label: 'Sign Out', danger: true },
          ].map((item, i) => (
            <button key={i} className={`w-full flex items-center justify-between p-4 rounded-xl transition-colors ${item.danger ? 'hover:bg-red-50 text-red-600' : 'hover:bg-slate-50 text-slate-700'}`}>
              <div className="flex items-center gap-4">
                <item.icon className="w-5 h-5" />
                <span className="font-medium">{item.label}</span>
              </div>
              <ChevronRight className={`w-5 h-5 ${item.danger ? 'text-red-300' : 'text-slate-300'}`} />
            </button>
          ))}
        </div>
      </div>
    </div>
  );

  const renderCall = () => (
    <div className="fixed inset-0 z-50 bg-slate-950 flex flex-col font-sans overflow-hidden">
      {/* End Call Modal overlay */}
      {showEndCallModal && (
        <div className="absolute inset-0 bg-black/60 backdrop-blur-sm z-[100] flex items-center justify-center p-4">
          <div className="bg-white rounded-3xl max-w-sm w-full p-6 shadow-2xl animate-fade-in-up">
            <h3 className="text-xl font-bold text-slate-900 mb-2">End Meeting?</h3>
            <p className="text-slate-500 mb-6 text-sm">As the host, you can end the meeting for everyone, or just leave and let others continue.</p>
            <div className="space-y-3">
              <button 
                onClick={() => setCallStatus('summary')}
                className="w-full bg-red-600 hover:bg-red-700 text-white font-bold py-3.5 rounded-xl transition-colors"
              >
                End Meeting for All
              </button>
              <button 
                onClick={() => setCallStatus('summary')}
                className="w-full bg-slate-100 hover:bg-slate-200 text-slate-700 font-bold py-3.5 rounded-xl transition-colors"
              >
                Leave Meeting
              </button>
              <button 
                onClick={() => setShowEndCallModal(false)}
                className="w-full text-slate-500 hover:text-slate-700 font-medium py-2 transition-colors"
              >
                Cancel
              </button>
            </div>
          </div>
        </div>
      )}

      {/* Header */}
      <header className="h-16 px-6 flex items-center justify-between border-b border-slate-800 bg-slate-900/50 backdrop-blur-md z-10">
        <div className="flex items-center gap-4">
          <div className="bg-red-500/10 text-red-500 px-3 py-1.5 rounded-full text-xs font-bold uppercase tracking-wider flex items-center gap-2">
            <span className="w-2 h-2 rounded-full bg-red-500 animate-pulse"></span>
            Transcribing
          </div>
          <span className="text-slate-300 font-medium border-l border-slate-700 pl-4 hidden sm:block">Q3 Planning Sync</span>
        </div>
        <div className="flex items-center gap-3">
           <div className="flex items-center gap-2 bg-slate-800 px-3 py-1.5 rounded-lg border border-slate-700">
             <Users className="w-4 h-4 text-slate-400" />
             <span className="text-sm font-medium text-slate-200">3</span>
           </div>
        </div>
      </header>

      {/* Main Content Area */}
      <div className="flex-1 flex overflow-hidden relative">
        
        {/* Dynamic Video/Screen Share Grid */}
        <div className="flex-1 p-4 flex flex-col gap-4 relative">
          
          {isScreenSharing ? (
            /* Screen Sharing Layout */
            <div className="flex-1 flex flex-col gap-4">
              {/* The Shared Screen */}
              <div className="flex-1 bg-slate-800 rounded-3xl overflow-hidden relative border-2 border-blue-500/50 shadow-lg shadow-blue-900/20 flex flex-col items-center justify-center group">
                <div className="absolute top-4 left-4 bg-blue-600 text-white text-xs font-bold px-3 py-1 rounded-full flex items-center gap-2 z-10">
                  <MonitorUp className="w-3 h-3" />
                  John Smith's Screen
                </div>
                {/* Mock Chart representation for screen share */}
                <div className="w-full max-w-lg p-8 bg-slate-900 rounded-2xl border border-slate-700 flex flex-col gap-6">
                  <div className="flex justify-between items-end">
                    <div className="w-16 h-32 bg-blue-500 rounded-t-sm"></div>
                    <div className="w-16 h-48 bg-blue-400 rounded-t-sm"></div>
                    <div className="w-16 h-24 bg-blue-600 rounded-t-sm"></div>
                    <div className="w-16 h-64 bg-blue-300 rounded-t-sm"></div>
                    <div className="w-16 h-40 bg-blue-500 rounded-t-sm"></div>
                  </div>
                  <div className="w-full h-px bg-slate-700"></div>
                  <div className="flex justify-between text-slate-400 text-xs font-mono">
                    <span>JAN</span><span>FEB</span><span>MAR</span><span>APR</span><span>MAY</span>
                  </div>
                </div>
              </div>
              
              {/* Minimized Participant Row */}
              <div className="h-32 flex gap-4 shrink-0">
                <div className="flex-1 bg-slate-800 rounded-2xl overflow-hidden relative border border-slate-700">
                  <img src="https://images.unsplash.com/photo-1573496359142-b8d87734a5a2?auto=format&fit=crop&w=400&q=80" alt="Jane" className="w-full h-full object-cover opacity-80"/>
                  <span className="absolute bottom-2 left-2 bg-black/60 text-white text-xs px-2 py-1 rounded">Jane Doe</span>
                </div>
                <div className="flex-1 bg-slate-800 rounded-2xl overflow-hidden relative border border-slate-700">
                  {isVideoOff ? (
                    <div className="w-full h-full flex items-center justify-center"><span className="text-slate-400">JD</span></div>
                  ) : (
                    <img src="https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?auto=format&fit=crop&w=400&q=80" alt="Self" className="w-full h-full object-cover transform -scale-x-100"/>
                  )}
                  <span className="absolute bottom-2 left-2 bg-black/60 text-white text-xs px-2 py-1 rounded">You</span>
                </div>
              </div>
            </div>
          ) : (
            /* Standard Video Layout */
            <>
              <div className="flex-1 bg-slate-800 rounded-3xl overflow-hidden relative border border-slate-700 group">
                <img 
                  src="https://images.unsplash.com/photo-1573496359142-b8d87734a5a2?auto=format&fit=crop&w=1200&q=80" 
                  alt="Main Speaker"
                  className="w-full h-full object-cover opacity-80"
                />
                <div className="absolute bottom-4 left-4 bg-slate-900/80 backdrop-blur-md px-4 py-2 rounded-xl border border-slate-700 flex items-center gap-3 shadow-lg">
                  <div className="w-8 h-8 rounded-full bg-blue-500 flex items-center justify-center text-white font-bold text-sm">
                    JD
                  </div>
                  <div>
                    <p className="text-white font-medium text-sm">Jane Doe</p>
                    <p className="text-slate-400 text-xs">Speaking...</p>
                  </div>
                </div>
              </div>
              
              {/* Picture in Picture (Self) */}
              <div className="absolute top-8 right-8 w-48 aspect-video bg-slate-800 rounded-2xl overflow-hidden border-2 border-slate-700 shadow-2xl z-20">
                {isVideoOff ? (
                    <div className="w-full h-full flex items-center justify-center bg-slate-900">
                      <div className="w-12 h-12 rounded-full bg-slate-800 flex items-center justify-center text-slate-400">
                        You
                      </div>
                    </div>
                ) : (
                    <img 
                      src="https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?auto=format&fit=crop&w=400&q=80" 
                      alt="Self view"
                      className="w-full h-full object-cover transform -scale-x-100"
                    />
                )}
                {isMuted && (
                  <div className="absolute top-2 right-2 bg-red-500 p-1.5 rounded-lg">
                    <MicOff className="w-3 h-3 text-white" />
                  </div>
                )}
              </div>
            </>
          )}
        </div>

        {/* Transcript Sidebar */}
        <div className={`w-80 bg-white border-l border-slate-200 flex flex-col transition-all duration-300 absolute right-0 top-0 bottom-0 z-30 md:relative ${showTranscript ? 'translate-x-0' : 'translate-x-full hidden'}`}>
          <div className="p-4 border-b border-slate-100 flex items-center justify-between bg-slate-50">
            <h3 className="font-semibold text-slate-800 flex items-center gap-2">
              <MessageSquare className="w-4 h-4 text-blue-600" />
              Live Transcript
            </h3>
            <button onClick={() => setShowTranscript(false)} className="md:hidden text-slate-400 p-1">
              <X className="w-5 h-5"/>
            </button>
          </div>
          <div className="flex-1 overflow-y-auto p-4 space-y-6 bg-white">
            {liveTranscript.map((msg, idx) => (
              <div key={idx} className="animate-fade-in-up">
                <div className="flex items-baseline justify-between mb-1">
                  <span className="font-semibold text-slate-900 text-sm">{msg.speaker}</span>
                  <span className="text-xs text-slate-400">{msg.time}</span>
                </div>
                <p className="text-sm text-slate-600 leading-relaxed bg-slate-50 p-3 rounded-2xl rounded-tl-none border border-slate-100">
                  {msg.text}
                </p>
              </div>
            ))}
            {liveTranscript.length < MOCK_TRANSCRIPT.length && (
               <div className="flex gap-1 items-center px-2 py-4">
                 <div className="w-2 h-2 rounded-full bg-blue-400 animate-bounce"></div>
                 <div className="w-2 h-2 rounded-full bg-blue-400 animate-bounce" style={{animationDelay: '150ms'}}></div>
                 <div className="w-2 h-2 rounde